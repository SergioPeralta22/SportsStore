import { Component } from '@angular/core';
/**
 * no hay codigo en el app.component.ts porque el componente principal solo existe para manejar el contenido mostrado al usuario
 */

@Component({
  selector: 'app',
  template: `<div class="bg-success p-2  text-center text-white">
    This is SportsStore
  </div>`,
})
export class AppComponent {}
